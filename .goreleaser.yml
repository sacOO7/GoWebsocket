project_name: gowebsocket
build:
  skip: true
checksum:
  name_template: checksums.txt
snapshot:
  name_template: '{{ .Tag }}-next'
changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
release:
  github:
    owner: sac007
    name: GoWebSocket

# add a source archive at release time
source:
  enabled: true